Soil feature builders.

from typing import List, Dict, Any


def compute_soil_features(tiles: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Return soil-derived features per tile (mock)."""
    return tiles
