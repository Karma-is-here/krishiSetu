NDVI feature builders.

from typing import List, Dict, Any


def compute_ndvi_features(tiles: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Return NDVI-derived features per tile (mock)."""
    return tiles
