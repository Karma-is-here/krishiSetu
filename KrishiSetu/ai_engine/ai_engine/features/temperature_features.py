Temperature feature builders.

from typing import List, Dict, Any


def compute_temperature_features(tiles: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Return temperature-derived features per tile (mock)."""
    return tiles
